{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/pacvar/master/assets/schema_input.json",
    "title": "nf-core/pacvar pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "errorMessage": "Sample ID must be provided and must be an integer or string that cannot contain spaces",
                "meta": ["id"],
                "type": ["string", "integer"],
                "pattern": "^\\S+$"
            },
            "bam": {
                "errorMessage": "BAM or CRAM file cannot contain spaces, must exist and has the extension '.bam' or '.cram'. This also is a required input.",
                "type": "string",
                "pattern": "^\\S+\\.(bam|cram)$",
                "format": "file-path",
                "exists": true
            },
            "pbi": {
                "errorMessage": "BAI or CRAI index file cannot contain spaces, must exist and has the extension '.bai', '.crai' or '.pbi'",
                "type": "string",
                "pattern": "^\\S+\\.(bai|crai|pbi)$",
                "format": "file-path",
                "exists": true
            }
        },
        "required": ["sample", "bam"]
    }
}
