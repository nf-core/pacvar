Changes in component 'nf-core/trgt/plot'
'modules/nf-core/trgt/plot/environment.yml' is unchanged
'modules/nf-core/trgt/plot/meta.yml' is unchanged
Changes in 'trgt/plot/main.nf':
--- modules/nf-core/trgt/plot/main.nf
+++ modules/nf-core/trgt/plot/main.nf
@@ -22,19 +22,40 @@
 
     script:
     def args = task.ext.args ?: ''
-    def prefix = task.ext.prefix ?: "${meta.id}_${repeat_id}"
-    // If user didn't specify an output path, plot to PNG
-    def output_arg = args.contains("--image") || args.contains("-o") ? "" : "--image ${prefix}.png"
 
     """
-    trgt plot \\
-        $args \\
-        --genome ${fasta} \\
-        --repeats ${repeats} \\
-        --spanning-reads ${bam} \\
-        --vcf ${vcf} \\
-        --repeat-id ${repeat_id} \\
-        $output_arg
+    if [ -z "${repeat_id}" ]; then
+        mapfile -t repeat_ids < <(awk -F '\\t' '{ split(\$4,a,";"); gsub("ID=","",a[1]); print a[1] }' "${repeats}")
+
+    else
+        IFS=',;' read -r -a repeat_ids <<< "${repeat_id}"
+    fi
+
+    for rid in "\${repeat_ids[@]}"; do
+
+        out_png_motifs="${meta.id}_\${rid}_motifs.png"
+        out_png_meth="${meta.id}_\${rid}_meth.png"
+
+        trgt plot \\
+            $args \\
+            --genome ${fasta} \\
+            --repeats ${repeats} \\
+            --spanning-reads ${bam} \\
+            --vcf ${vcf} \\
+            --repeat-id "\${rid}" \\
+            --show motifs \\
+            --image \$out_png_motifs
+
+        trgt plot \\
+            $args \\
+            --genome ${fasta} \\
+            --repeats ${repeats} \\
+            --spanning-reads ${bam} \\
+            --vcf ${vcf} \\
+            --repeat-id "\${rid}" \\
+            --show meth \\
+            --image \$out_png_meth
+    done
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":

'modules/nf-core/trgt/plot/tests/nextflow.config' is unchanged
'modules/nf-core/trgt/plot/tests/main.nf.test' is unchanged
'modules/nf-core/trgt/plot/tests/main.nf.test.snap' is unchanged
************************************************************
